import { createSlice } from "@reduxjs/toolkit";

const initialState={
    orderDetails:[]
}

export const orderDetailSlice=createSlice({
    name: 'orderDetails',
    initialState,
    reducers:{
        
    }
})